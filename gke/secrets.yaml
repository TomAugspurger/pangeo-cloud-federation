# Fill in these tokens with openssl rand -hex 32
jupyterhub:
  hub:
    services:
      dask-gateway:
        apiToken: "<token-1>"
  proxy:
    secretToken: "<token-2>"

dask-gateway:
  gateway:
    auth:
      jupyterhub:
        apiToken: "<token-1>"

